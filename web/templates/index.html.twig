<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Homepage</title>
    </head>
    <body>
        <p>
            <strong>HomePage</strong>

            <br />

            This homepage is accessible to everyone, logged-in or otherwise

            <br />
            <br />

            {% if is_granted('ROLE_USER') %}
                Currently logged in as: {{ user.getEmail() }}, {{ user.getEmail() }}
                <br />
                <a href="{{ path('logout') }}">Logout</a>
            {% else %}
                <a href="{{ path('login') }}">Login</a>
            {% endif %}
        </p>
    </body>
</html>

